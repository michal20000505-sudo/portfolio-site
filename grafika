!DOCTYPE html
html lang=pl
head
    meta charset=UTF-8
    meta name=viewport content=width=device-width, initial-scale=1.0
    titleMichał Jarosiński - Projekty Graficznetitle
    link rel=preconnect href=httpsfonts.googleapis.com
    link rel=preconnect href=httpsfonts.gstatic.com crossorigin
    link href=httpsfonts.googleapis.comcss2family=Space+Groteskwght@300;500;700&display=swap rel=stylesheet
    
    style
         --- ZMIENNE KOLORYSTYCZNE (SPÓJNE Z INDEX.HTML) --- 
        root {
            --bg-color #050505;
            --text-color #ffffff;
            --cyan #00ffff;
            --magenta #ff00ff;
            --yellow #ffff00;
            --font-main 'Space Grotesk', sans-serif;
            --transition all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }

         --- RESET & BASE --- 
         { margin 0; padding 0; box-sizing border-box; cursor none; }
        body { background-color var(--bg-color); color var(--text-color); font-family var(--font-main); overflow-x hidden; line-height 1.6; position relative; }
        a { text-decoration none; color inherit; }
        ul { list-style none; }
        img { display block; width 100%; height auto; }

         --- UI ELEMENTS --- 
        #starfield { position fixed; top 0; left 0; width 100%; height 100%; z-index -1; pointer-events none; }
        
        .cursor { width 20px; height 20px; border 2px solid var(--text-color); border-radius 50%; position fixed; pointer-events none; z-index 9999; transform translate(-50%, -50%); transition transform 0.1s ease, background-color 0.2s; mix-blend-mode difference; }
        .cursor.hovered { transform translate(-50%, -50%) scale(2.5); background-color var(--text-color); border-color transparent; }

        #preloader { position fixed; top 0; left 0; width 100%; height 100%; background var(--bg-color); z-index 10000; display flex; justify-content center; align-items center; transition opacity 0.5s ease; }
        .loader-text { font-size 2rem; font-weight 700; animation pulseCMYK 1s infinite alternate; }
        @keyframes pulseCMYK { 0% { text-shadow 2px 2px 0 var(--cyan); } 50% { text-shadow -2px 2px 0 var(--magenta); } 100% { text-shadow -2px -2px 0 var(--yellow); } }

        .container { max-width 1400px; margin 0 auto; padding 0 40px; }

         --- HEADER --- 
        header { position fixed; top 0; width 100%; padding 30px 0; z-index 100; mix-blend-mode difference; }
        nav { display flex; justify-content space-between; align-items center; }
        .logo { font-weight 700; font-size 1.5rem; letter-spacing -1px; } 
        .logo span { color var(--cyan); }
        .nav-links { display flex; gap 50px; }
        .nav-links a { font-size 0.9rem; text-transform uppercase; letter-spacing 2px; position relative; }
        .nav-links aafter { content ''; position absolute; bottom -5px; left 0; width 0; height 2px; background var(--magenta); transition var(--transition); }
        .nav-links ahoverafter { width 100%; }
        .nav-links a.active { color var(--cyan); }
        .nav-links a.activeafter { width 100%; background var(--cyan); }

         --- HERO MINI --- 
        #page-hero { padding 200px 0 100px; text-align center; }
        .page-title { font-size 6vw; font-weight 700; line-height 1; text-transform uppercase; margin-bottom 20px; color var(--text-color); letter-spacing -2px; }
        .page-title span { color transparent; -webkit-text-stroke 1px var(--magenta); }
        .page-subtitle { color #888; font-size 1.2rem; letter-spacing 4px; text-transform uppercase; }

         --- MASONRY GRID STYLES --- 
        .gallery-section { padding-bottom 150px; }
        
        .masonry-grid {
            column-count 3;
            column-gap 40px;
        }

        .masonry-item {
            break-inside avoid;
            margin-bottom 40px;
            position relative;
        }

        .work-card {
            display block;
            position relative;
            margin-bottom 15px;
            border 1px solid #222;
            overflow hidden;
            transition var(--transition);
        }

         Hover Effect 
        .work-cardafter {
            content '';
            position absolute;
            top 0; left 0; width 100%; height 100%;
            background linear-gradient(45deg, rgba(0,255,255,0.2), rgba(255,0,255,0.2));
            opacity 0;
            transition var(--transition);
        }

        .work-cardhover { transform translateY(-5px); border-color var(--cyan); box-shadow 0 10px 30px -10px rgba(0, 255, 255, 0.15); }
        .work-cardhoverafter { opacity 1; }
        .work-card img { transition transform 0.6s cubic-bezier(0.165, 0.84, 0.44, 1); }
        .work-cardhover img { transform scale(1.05); }

        .work-info h3 { font-size 1.4rem; font-weight 500; margin-bottom 5px; color #fff; }
        .work-info .category { color var(--magenta); font-size 0.8rem; text-transform uppercase; letter-spacing 2px; font-weight 700; display inline-block; margin-bottom 10px; }
        .work-info p { color #888; font-size 0.95rem; font-weight 300; line-height 1.4; }

         --- FOOTER --- 
        footer { padding 50px 0; text-align center; border-top 1px solid #222; color #666; font-size 0.8rem; background-color var(--bg-color); }

         --- RESPONSIVE --- 
        @media (max-width 1024px) {
            .masonry-grid { column-count 2; column-gap 20px; }
            .page-title { font-size 5rem; }
        }
        @media (max-width 768px) {
            .masonry-grid { column-count 1; }
            .nav-links { gap 20px; }
            .nav-links a { font-size 0.75rem; }
            .page-title { font-size 3.5rem; }
        }
    style
head
body

    canvas id=starfieldcanvas
    div class=cursordiv
    div id=preloaderdiv class=loader-textLOADING...divdiv

    header
        div class=container
            nav
                !-- Link powrotny do strony głównej --
                a href=index.html class=logo link-hoverMJspan.spana
                div class=nav-links
                    a href=index.html#about class=link-hoverO mniea
                    a href=index.html#work class=link-hoverWeba
                    !-- Oznaczamy tę stronę jako aktywną w menu --
                    a href=# class=active link-hoverGrafikaa
                    a href=index.html#contact class=link-hoverKontakta
                div
            nav
        div
    header

    div id=page-hero
        div class=container
            h1 class=page-title revealVisualbrspanWorksspanh1
            p class=page-subtitle revealBranding  Print  Art Directionp
        div
    div

    section class=gallery-section
        div class=container
            !-- MASONRY GRID START --
            div class=masonry-grid

                !-- 1. PLAKAT LIPALI (PION) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=Lipali-koncert-roboczy-PLAKAT-3.jpg alt=Lipali Plakat Koncertowy
                    a
                    div class=work-info
                        span class=categoryPoster Artspan
                        h3Lipali Concert Tourh3
                        pProjekt plakatu promującego trasę koncertową. Minimalistyczna typografia połączona z fotografią zespołu.p
                    div
                div

                !-- 2. GROM ART ULOTKA (PION) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=Grom-art-b5-front.jpg alt=Grom Art Statuetka
                    a
                    div class=work-info
                        span class=categoryPrint & 3Dspan
                        h3Grom Art Operatorh3
                        pProjekt ulotki B5 oraz wizualizacja statuetki operatora jednostki specjalnej.p
                    div
                div

                !-- 3. KONTROLER (POZIOMSQUARE) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=digicamoblue-powder.jpg alt=Custom PS4 Controller
                    a
                    div class=work-info
                        span class=categoryProduct Designspan
                        h3Digicamo Blueh3
                        pProjekt graficzny customowego kontrolera oraz kompozycja reklamowa dla AIM Controllers.p
                    div
                div

                !-- 4. WIZYTÓWKA SUDBAL (POZIOM) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=Wizytowka-9x5-sudbal.jpg alt=Sudbal Garage
                    a
                    div class=work-info
                        span class=categoryBrandingspan
                        h3Sudbal Garageh3
                        pProjekt wizytówki dla serwisu detailingowego. Elegancka czerń i chromowane wykończenie logo.p
                    div
                div

                !-- 5. WIZYTÓWKA TRENER (POZIOM) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=Wizytowka-9x5.jpg alt=Wizytówka Trenera
                    a
                    div class=work-info
                        span class=categoryBrandingspan
                        h3Personal Trainerh3
                        pIdentyfikacja wizualna trenera personalnego. Energetyczna zieleń i mocny, sportowy krój pisma.p
                    div
                div

                !-- 6. WIZYTÓWKI GROM MOCKUP (POZIOM) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=render2.jpg alt=Grom Art Branding
                    a
                    div class=work-info
                        span class=categoryBrandingspan
                        h3Grom Art Identityh3
                        pKompleksowy branding dla firmy tworzącej statuetki militarne. Mockup wizytówek premium.p
                    div
                div

                !-- 7. WIZYTÓWKA SCHMIDT (POZIOM) --
                div class=masonry-item reveal
                    a href=# class=work-card link-hover
                        img src=wizytowka-max-bucz.jpg alt=Schmidt Maszyny
                    a
                    div class=work-info
                        span class=categoryBrandingspan
                        h3Schmidt Constructionh3
                        pProjekt wizytówki dla firmy budowlanej. Czytelny układ z wykorzystaniem ilustracji maszyn.p
                    div
                div

            div
            !-- MASONRY GRID END --
        div
    section

    footer
        div class=container&copy; 2025 Michał Jar Portfolio. Designed with CMYK passion.div
    footer

    !-- SKRYPTY (IDENTYCZNE JAK NA GŁÓWNEJ) --
    script
         PRELOADER
        const preloader = document.getElementById('preloader');
        function hidePreloader() { 
            if(preloader.style.opacity!=='0'){
                preloader.style.opacity='0'; 
                setTimeout(()={preloader.style.display='none'},500);
            } 
        }
        window.addEventListener('load', ()={setTimeout(hidePreloader,500)});
         Fallback
        setTimeout(hidePreloader, 3000);

         REVEAL ANIMATION
        const revealObserver = new IntersectionObserver((entries) = {
            entries.forEach(entry = { 
                if (entry.isIntersecting) entry.target.classList.add('active'); 
            });
        }, { threshold 0.1 });
        
        document.querySelectorAll('.reveal').forEach(el = revealObserver.observe(el));

         STARFIELD BACKGROUND
        const canvas = document.getElementById('starfield');
        const ctx = canvas.getContext('2d');
        let width, height, stars = [];
        const mouse = { x undefined, y undefined };
        const colors = ['#00ffff', '#ff00ff', '#ffff00', '#ffffff', '#ffffff']; 

        function resize() { 
            width=window.innerWidth; 
            height=window.innerHeight; 
            canvas.width=width; 
            canvas.height=height; 
            initStars(); 
        }
        
        class Star {
            constructor() {
                this.x = Math.random()  width; 
                this.y = Math.random()  height;
                this.size = Math.random()  2;
                this.density = (Math.random()  20) + 1;
                this.color = colors[Math.floor(Math.random()  colors.length)];
                this.vx = (Math.random() - 0.5)  0.5; 
                this.vy = (Math.random() - 0.5)  0.5;
                this.opacity = Math.random(); 
                this.twinkle = Math.random()  0.02 + 0.005;
            }
            draw() { 
                ctx.fillStyle=this.color; 
                ctx.globalAlpha=this.opacity; 
                ctx.beginPath(); 
                ctx.arc(this.x,this.y,this.size,0,Math.PI2); 
                ctx.fill(); 
                ctx.globalAlpha=1; 
            }
            update() {
                this.x+=this.vx; 
                this.y+=this.vy;
                if(this.x0)this.x=width; 
                if(this.xwidth)this.x=0; 
                if(this.y0)this.y=height; 
                if(this.yheight)this.y=0;
                
                 Mouse interaction
                if(mouse.x){
                    let dx=mouse.x-this.x, dy=mouse.y-this.y, dist=Math.sqrt(dxdx+dydy);
                    if(dist150){
                        let f=(150-dist)150;
                        this.x-=(dxdist)fthis.density2; 
                        this.y-=(dydist)fthis.density2;
                    }
                }
                this.opacity+=this.twinkle; 
                if(this.opacity1this.opacity0.2)this.twinkle=-this.twinkle;
            }
        }
        function initStars(){ 
            stars=[]; 
            for(let i=0;i(widthheight)8000;i++) stars.push(new Star()); 
        }
        function animateStars(){ 
            ctx.clearRect(0,0,width,height); 
            stars.forEach(s={s.draw();s.update()}); 
            requestAnimationFrame(animateStars); 
        }
        
        window.addEventListener('resize', resize);
        window.addEventListener('mousemove', e={mouse.x=e.x; mouse.y=e.y});
        
         CURSOR
        const cursor=document.querySelector('.cursor');
        document.addEventListener('mousemove', e={
            cursor.style.left=e.clientX+'px'; 
            cursor.style.top=e.clientY+'px';
        });
        
         Hover effects handler
        document.querySelectorAll('.link-hover, a, .work-card').forEach(l={
            l.addEventListener('mouseenter',()=cursor.classList.add('hovered'));
            l.addEventListener('mouseleave',()=cursor.classList.remove('hovered'));
        });

        resize(); 
        animateStars();
    script
body
html
